<?php
    // 可变变量
    // $pname = '1';
    // $arr = array(1,23,4,5,67);
    // echo $arr[$pname];

    // $pname = 'number';
    // $number = 123;

    // $$pname  = 456;
    // echo $$pname;
    // var_dump($$pname);
    // var_dump($number);
    // $number = 11;
    // var_dump($$pname);
    // var_dump($number);

    // 外部变量
    // $_GET['num']; // 获取get传值的结果
    // $_POST['pwd']; //获取POST传值的结果
    // $_COOKIE['info']; // 获取会话控制中cookie传值
    // $_SESSION['Session']; //获取会话控制中session的值
    // $_REQUEST; // 既能获取get，也能获取post中的值

    // echo $_SERVER['SCRIPT_FILENAME'];
    // phpinfo();

    defined('AUTH') or exit('false');
?>